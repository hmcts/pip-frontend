nodejs:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    SECRETS_DIRECTORY: /mnt/secrets/pip-ss-kv-stg
    OIDC: true
    FRONTEND_URL: https://${SERVICE_FQDN}
