nodejs:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    API_URL: pip-business-rules.staging.platform.hmcts.net
    FRONTEND_URL: https://pip-frontend.staging.platform.hmcts.net