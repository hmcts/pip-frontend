{% from "macros/common-components.njk" import goBack, submitButton %}
{% from "govuk/components/table/macro.njk" import govukTable %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
  {% from "govuk/components/panel/macro.njk" import govukPanel %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}
{% extends "template.njk" %}
{% block pageTitle %}
 Media Account Rejection
{% endblock %}
{% block beforeContent %}
  {{ super() }}
  {{ goBack() }}
{% endblock %}
{% block content %}

  {{ govukPanel({
    titleText: panelHeading
  }) }}

  {% set tableRows = [] %}
  {% for i in range(0, 5) %}
  {% set _ = tableRows.push([
      {
        text: tableHeaders[i]
      },
      {
        text: applicantData[i]
      }
  ]) %}
  {% endfor %}

  {{ govukTable({
    caption: tableCaption,
    captionClasses: "govuk-table__caption--m",
    firstCellIsHeader: true,
    rows: tableRows
  }) }}

  <h1 class="govuk-heading-m">{{ whatHappensNextHeader }}</h1>
  <p class="govuk-body">{{ whatHappensNext1 }} ((mailto link Goes Here)) {{ whatHappensNext2 }}</p>

  <h1 class = "govuk-heading-m">{{ emailTemplateHeader }}</h1>

  {{ govukInsetText ({
    html: emailTemplateContent
  }) }}





{% endblock %}
